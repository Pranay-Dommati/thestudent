Django>=4.2.0,<6.1  # DRF 3.17.2 only supports up to Django 6.0; 6.1 dropped django.utils.cache.cc_delim_re
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.0.0
whitenoise>=6.7.0
python-dotenv>=1.0.0
Pillow>=10.0.0
requests>=2.31.0
scikit-learn>=1.3.0
numpy>=1.24.0
social-auth-app-django>=5.4.0
mysqlclient>=2.2.0
PyMySQL>=1.1.1
psycopg2-binary>=2.9.9
reportlab>=4.0.0
PyPDF2>=3.0.0
pytest>=8.2.0
pytest-django>=4.8.0
gunicorn>=21.2.0
gevent>=23.9.0

# AWS SES for email delivery
boto3>=1.40.0

# Persistent media storage (Cloudinary)
cloudinary>=1.39.1
django-cloudinary-storage>=0.3.0

# Code Visualizer AI narration
google-genai>=1.0.0

# Redis cache for DRF throttling (production)
django-redis>=5.4.0

# Razorpay for payments
razorpay>=1.4.0

# Celery for background tasks
celery>=5.3.0
redis>=5.0.0

# Memory monitoring (used in scrib pdf_generation to log RSS per batch)
psutil>=5.9.0

# YouTube transcript fetching for the "From YouTube" flow (transcript-first path).
# Scrapes YouTube's timedtext endpoint - no API key, but datacenter IPs are
# often blocked, which is why the video pipeline remains the fallback.
youtube-transcript-api>=1.2.4
